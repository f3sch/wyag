#pragma once

#include <cstddef>
#include <cstdlib>

namespace libwyag {

const size_t ERROR_IN_REPO = 2;
const size_t ERROR_IN_COMMAND_LINE = EXIT_FAILURE;
const size_t SUCCESS = EXIT_SUCCESS;

}  // namespace libwyag
